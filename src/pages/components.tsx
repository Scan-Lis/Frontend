import SearchBarAutocomplete from "@/components/search-bar-autocomplete/search-bar-autocomplete";

const Components = () => {
  return (
    <main className="p-8">
      <SearchBarAutocomplete />
    </main>
  );
};

export default Components;
